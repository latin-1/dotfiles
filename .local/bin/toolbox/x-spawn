#!/bin/bash
set -eo pipefail

runner=(host-spawn)
runner+=("$SHELL" -c 'exec "$@"' --)
runner+=(env GHOSTTY_RESOURCES_DIR="$GHOSTTY_RESOURCES_DIR")

program="$(basename "$0")"
if [[ $program == x-spawn ]]; then
  "${runner[@]}" "$@"
else
  "${runner[@]}" "$program" "$@"
fi
